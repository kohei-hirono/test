# くじ引き Webアプリ

ブラウザで動くシンプルなくじ引きアプリです。

## 機能

- くじプールをブラウザ上で自由に編集
- 重複ありで抽選
- 重複なしで抽選（残り件数表示あり）
- 抽選履歴の表示
- プール設定のローカル保存

## 使い方

`index.html` をブラウザで開くだけで利用できます。

ローカルサーバーで確認する場合:

```bash
python3 -m http.server 8000
```

その後 `http://localhost:8000` を開いてください。
